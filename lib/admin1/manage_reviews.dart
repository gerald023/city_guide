import 'package:flutter/material.dart';

class ManageReviews extends StatelessWidget {
  const ManageReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('List of reviews with Approve/Delete options'),
    );
  }
}
