import 'package:flutter/material.dart';

class ManageNotifications extends StatelessWidget {
  const ManageNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notification creation and history'),
    );
  }
}
